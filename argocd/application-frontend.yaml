apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-frontend
  namespace: argocd
  labels:
    app.kubernetes.io/name: demo-frontend
    app.kubernetes.io/part-of: demo-app
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    # TODO: Replace <GIT_REPO_URL> with your Git repository URL
    # Example: https://github.com/your-org/demo-ci-cd-eks
    repoURL: <GIT_REPO_URL>
    targetRevision: main
    path: helm/frontend

    helm:
      # Values overrides for frontend - adjust as needed
      parameters:
        - name: image.repository
          value: <AWS_ACCOUNT_ID>.dkr.ecr.<AWS_REGION>.amazonaws.com/demo-frontend
        - name: image.tag
          value: latest

  destination:
    server: https://kubernetes.default.svc
    namespace: demo

  syncPolicy:
    # NOTE: Automated sync is convenient for demo purposes.
    # For production, consider manual sync or protected branches with approval workflows.
    automated:
      prune: true      # Automatically delete resources removed from git
      selfHeal: true  # Automatically sync to match git state

    syncOptions:
      - CreateNamespace=true

    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
